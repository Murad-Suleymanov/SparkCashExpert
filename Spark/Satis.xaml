<UserControl x:Class="Spark.Satis"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dtg_Selling" HorizontalAlignment="Left"  ItemsSource="{Binding}" Grid.Row="0"  VerticalAlignment="Top"  
                  VerticalGridLinesBrush="LightBlue" HorizontalGridLinesBrush="Black" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Id}" Header="MyID" Width="auto" />
                <DataGridTextColumn Binding="{Binding Path=Name}" Header="Name" Width="*" />
            </DataGrid.Columns>
            <DataGrid.Resources>
                <Style BasedOn="{StaticResource {x:Type DataGridColumnHeader}}" TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="Background" Value="#342e37" />
                </Style>
            </DataGrid.Resources>
        </DataGrid>
        <Button Grid.Row="1" Content="Təstiqlə" HorizontalAlignment="Right" Margin="0,10,0,0"  VerticalAlignment="Top" Width="85"/>
    </Grid>
</UserControl>
