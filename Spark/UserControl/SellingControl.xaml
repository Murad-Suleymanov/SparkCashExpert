<UserControl x:Class="Spark.SellingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dtg_Selling" HorizontalAlignment="Left"  ItemsSource="{Binding}" Grid.Row="0"  VerticalAlignment="Top"></DataGrid>
        <Button Grid.Row="1"  Content="Təsdiqlə" HorizontalAlignment="Right" Margin="0,10,0,0" Background="#9fd356"  VerticalAlignment="Top" Width="85" BorderBrush="#9fd356" Foreground="Black"/>
    </Grid>
</UserControl>
