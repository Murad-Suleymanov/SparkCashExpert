<Window x:Class="Spark.View.CalculateRemainder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CalculateRemainder" Height="400" Width="700" WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="btnChoose" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#9fd356"/>
            <Setter Property="Margin" Value="6,10,6,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Content" Value="SEÇ"/>
            <Setter Property="Width" Value="280"/>
        </Style>
        <Style x:Key="btnRight" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#9fd356"/>
            <Setter Property="Height" Value="70"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Margin" Value="10,5,20,5"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="btnLeft" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#9fd356"/>
            <Setter Property="Height" Value="70"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Margin" Value="20,5,5,5"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="btnCenter" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#9fd356"/>
            <Setter Property="Height" Value="70"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Margin" Value="10,5,10,5"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>
    <Grid Background="#342e37">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="20,0,20,0">
                <Label Content="Yekun" FontWeight="Bold" FontSize="20" Foreground="White" />
                <TextBox Text="{Binding TotalSum,Mode=OneWay}" Background="White" Height="40" IsReadOnly="True"  FontSize="20"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Margin="20,0,20,0">
                <Label Content="Nağd ödəniş" FontWeight="Bold" FontSize="20" Foreground="White" />
                <TextBox Text="{Binding WithCash,Mode=TwoWay}" Background="White" Height="40"  FontSize="20"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Margin="20,0,20,0">
                <Label Content="Nağdsız ödəniş" FontWeight="Bold" FontSize="20" Foreground="White"/>
                <TextBox Text="{Binding WithCart,Mode=TwoWay}" Background="White" Height="40"  FontSize="20"/>
            </StackPanel>
            <StackPanel Grid.Row="3" Margin="20,0,20,0">
                <Label Content="Qalıq" FontWeight="Bold" FontSize="20" Foreground="White" />
                <TextBox Text="{Binding Remainder,Mode=OneWay}" Background="White" Height="40" IsReadOnly="True" FontSize="20"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Row="0" Grid.Column="0" Style="{StaticResource btnLeft}" Content="1" />
            <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource btnCenter}" Content="2"/>
            <Button Grid.Row="0" Grid.Column="2" Style="{StaticResource btnRight}" Content="3"/>
            <Button Grid.Row="1" Grid.Column="0" Style="{StaticResource btnLeft}" Content="4"/>
            <Button Grid.Row="1" Grid.Column="1" Style="{StaticResource btnCenter}" Content="5"/>
            <Button Grid.Row="1" Grid.Column="2" Style="{StaticResource btnRight}" Content="6"/>
            <Button Grid.Row="2" Grid.Column="0" Style="{StaticResource btnLeft}" Content="7"/>
            <Button Grid.Row="2" Grid.Column="1" Style="{StaticResource btnCenter}" Content="8"/>
            <Button Grid.Row="2" Grid.Column="2" Style="{StaticResource btnRight}" Content="9"/>
            <Button Grid.Row="3" Grid.Column="0" Style="{StaticResource btnLeft}" Content="00"/>
            <Button Grid.Row="3" Grid.Column="1" Style="{StaticResource btnCenter}" Content="0"/>
            <Button Grid.Row="3" Grid.Column="2" Style="{StaticResource btnRight}" Content="C" Command="{Binding cancelCommand}"/>
            <Button Command="{Binding confirmInvoiceCommand}" Grid.Row="4" Grid.ColumnSpan="3" Style="{StaticResource btnChoose}"/>
        </Grid>
    </Grid>
</Window>
